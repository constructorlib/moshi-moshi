import { default as _btn } from "react-bootstrap/Button";
import Modal from "react-bootstrap/Modal";
import styled from "styled-components";

export const Container = styled(Modal)`
  padding: 1rem;
  backdrop-filter: blur(5px);
`;
